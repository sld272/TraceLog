"""TraceLog release version.

Single Python-side source of truth. Keep in sync with the release tag
and the Node-side versions in frontend/package.json and
desktop/package.json (electron-builder and the desktop update check
read the latter).
"""

APP_VERSION = "0.1.0"
